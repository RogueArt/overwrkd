pub mod user_io;
use user_io::get_num_seconds;

pub mod timer;
use timer::Timer;

fn main() {
    
    // let seconds = get_num_seconds();
    // let mut timer = Timer::new(seconds);
    // timer.start();
}
